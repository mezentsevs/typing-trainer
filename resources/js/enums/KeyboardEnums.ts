export enum Language {
    En = 'en',
    Ru = 'ru',
}

export enum SpecialPosition {
    TopLeft = 'top-left',
    TopRight = 'top-right',
}

export enum Zone {
    Left = 'left',
    Right = 'right',
}
